{
  "type": "module",
  "dependencies": {
    "dotenv": "^16.4.5",
    "firebase": "^10.13.0"
  },
  "scripts": {
    "build": "npm run build:firebase && npm run build:netlify"
  },
  "devDependencies": {
    "netlify-cli": "latest"
  }
}